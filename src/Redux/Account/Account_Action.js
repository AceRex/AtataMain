import * as actionTypes from './Account_types'

export const LOGIN = (user) => {
    return{
        type: actionTypes.LOGIN,
        payload: {
            user: null
        }
    }
}
export const LOGOUT = (user) => {
    return{
        type: actionTypes.LOGIN,
        payload: {
            user: null
        }
    }
}
export const REGISTER = (user) => {
    return{
        type: actionTypes.LOGIN,
        payload: {
            user: null
        }
    }
}